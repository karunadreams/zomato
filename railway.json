{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn milestone1.phase5_api.app:app --host 0.0.0.0 --port $PORT --app-dir src",
    "restartPolicyType": "ON_FAILURE"
  }
}
